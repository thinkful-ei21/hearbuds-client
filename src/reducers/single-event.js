import {
    action
} from '../actions/single-events'

const initialState = {
    'name': 'Blink 182',
    'venue': 'EMpire Control Room',
    'date': 'Sept. 24',
    'img': [],
    'description': '',
    'links': ''
    
    }


export default function reducer(state = initialState, action) {
    
    return state;
}